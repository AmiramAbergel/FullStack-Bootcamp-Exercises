import React from 'react';

const LargeHeading = (props) => {
    return <h2>{props.children}</h2>; //props added and fixed to be props.children
};

export default LargeHeading; // moved here and changed to default
