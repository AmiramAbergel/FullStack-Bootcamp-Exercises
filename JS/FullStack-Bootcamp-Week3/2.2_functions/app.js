function tellFortune(jobTitle,location,partnerName,numberOfChildren) {
    let res = `You will be a  ${jobTitle} in ${location} married to ${partnerName} with ${numberOfChildren} children.`
    return res;
}